diff -ruN prosafe-vlan-old/prosafe/__main__.py prosafe-vlan-new/prosafe/__main__.py
--- prosafe-vlan-old/prosafe/__main__.py	2026-08-15 14:36:37.038611068 +0200
+++ prosafe-vlan-new/prosafe/__main__.py	2026-08-13 23:59:01.353052717 +0200
@@ -1,3 +1,4 @@
+import os
 import traceback
 from pathlib import Path
 
@@ -5,6 +6,8 @@
 
 from .vlan_config import load_config
 from .cli import RequiredIf
+from .switches import SwitchModel
+from .switches.general import validate_password
 
 
 @click.group()
@@ -20,21 +23,40 @@
 @click.option('--savepath', cls=RequiredIf, required_if="norestore",
             type=click.Path(exists=True, file_okay=False, dir_okay=True, writable=True),
             help='If specified, save switch config backups to that folder.')
-def apply(config: str, norestore: bool, savepath: str|None):
+@click.option('--change-pw-allowed', is_flag=True, default=False,
+            help='If the configured password is rejected, log in with the factory default password and change it.')
+def apply(config: str, norestore: bool, savepath: str|None, change_pw_allowed: bool):
     click.echo("Loading config from %s ..." % config)
     cfgs = load_config(config)
     click.echo("Got %d switch(es)." % len(cfgs))
-    
+
+    env_password = os.environ.get("PROSAFE_VLAN_PASSWORD")
+
     if isinstance(savepath, str):
         savepath = savepath.rstrip('/')
         savepath = Path(savepath)
-    
+
     for sw_name, sw_cfg in cfgs.items():
         click.echo("Processing switch '%s' ..." % sw_name)
-        sw = sw_cfg.model.driver(sw_cfg.address, sw_cfg.password)
+        password = env_password or sw_cfg.password
+        sw = sw_cfg.model.driver(sw_cfg.address, password)
         backup_data: bytes|None = None
 
-        with sw.logged_in():
+        try:
+            sw.login()
+        except Exception:
+            if not change_pw_allowed:
+                raise
+            click.echo("Configured password rejected; changing from the factory default ...")
+            default_sw = sw_cfg.model.driver(sw_cfg.address, "password")
+            default_sw.login()
+            default_sw.change_default_password(password)
+            default_sw.logout()
+            sw.login()
+
+        sw.enable_8021q_mode()
+
+        try:
             backup_data = sw.backup()
             if isinstance(savepath, Path):
                 backup_file = savepath / f'{sw_name}.cfg'
@@ -52,7 +74,37 @@
                     click.echo("Restoring switch(%s) configuration ..." % sw_name)
                     sw.restore(backup_data)
                 break
-        
+        finally:
+            sw.logout()
+
     click.echo("All done!")
 
+
+@cli.command('change-password')
+@click.option('--address', '-a', required=True, help='Switch address or hostname.')
+@click.option('--model', '-m', required=True,
+              type=click.Choice(['gs108ev3', 'gs116ev2']),
+              help='Switch model (driver).')
+@click.option('--old-password', '-o', default='password',
+              help='Current admin password (default: PROSAFE_VLAN_OLD_PASSWORD, then factory "password").')
+@click.option('--new-password', '-n', default=None,
+              help='New admin password (default: PROSAFE_VLAN_NEW_PASSWORD).')
+def change_password_cmd(address, model, old_password, new_password):
+    old_password = os.environ.get("PROSAFE_VLAN_OLD_PASSWORD") or old_password
+    new_password = new_password or os.environ.get("PROSAFE_VLAN_NEW_PASSWORD")
+    if not new_password:
+        raise click.ClickException("--new-password or PROSAFE_VLAN_NEW_PASSWORD is required")
+    if not validate_password(new_password):
+        raise click.ClickException(
+            "New password does not meet the switch policy: "
+            "6-20 chars, one uppercase, one lowercase, one digit, "
+            "allowed symbols !@#$%^&*()"
+        )
+    sw = SwitchModel(model).driver(address, old_password)
+    sw.login()
+    sw.change_password(old_password, new_password)
+    sw.logout()
+    click.echo("Password changed successfully.")
+
+
 cli()
\ No newline at end of file
diff -ruN prosafe-vlan-old/prosafe/switches/general.py prosafe-vlan-new/prosafe/switches/general.py
--- prosafe-vlan-old/prosafe/switches/general.py	2026-08-15 14:36:37.038738728 +0200
+++ prosafe-vlan-new/prosafe/switches/general.py	2026-08-13 23:50:19.073522117 +0200
@@ -1,3 +1,4 @@
+import re
 from enum import IntEnum
 from typing import Dict
 from contextlib import contextmanager
@@ -6,6 +7,14 @@
 VlanId = int
 PortId = int
 
+# Same policy as the switch's checkValidPassword() JS:
+# 6-20 chars, at least one digit/lower/upper, symbols !@#$%^&*() only.
+PASSWORD_PATTERN = re.compile(r"^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])[0-9a-zA-Z!@#$%^&*()]{6,20}$")
+
+
+def validate_password(password: str) -> bool:
+    return bool(PASSWORD_PATTERN.match(password))
+
 
 class VlanPortMembership(IntEnum):
     # These values are from GS108Ev3's web interface
@@ -66,3 +75,15 @@
     def fetch_statistics(self) -> Dict:
         """fetch latest ports statistic"""
         raise NotImplementedError()
+
+    def change_default_password(self, new_password: str):
+        """change the switch admin password from the factory default"""
+        raise NotImplementedError()
+
+    def change_password(self, old_password: str, new_password: str):
+        """change the switch admin password"""
+        raise NotImplementedError()
+
+    def enable_8021q_mode(self):
+        """enable advanced 802.1Q VLAN mode"""
+        raise NotImplementedError()
diff -ruN prosafe-vlan-old/prosafe/switches/gs108ev3/consts.py prosafe-vlan-new/prosafe/switches/gs108ev3/consts.py
--- prosafe-vlan-old/prosafe/switches/gs108ev3/consts.py	2026-08-15 14:36:37.038824540 +0200
+++ prosafe-vlan-new/prosafe/switches/gs108ev3/consts.py	2026-08-13 23:29:38.485391287 +0200
@@ -21,6 +21,12 @@
 
 SW_INFO = '/switch_info.htm'
 
+SW_PWD_CK = '/pwd_ck.htm'
+SW_CHANGE_DEFAULT_PWD = '/changeDefPwd.cgi'
+SW_USER_PWD_PAGE = '/user.htm'
+SW_USER_PWD = '/user.cgi'
+SW_8021Q_ENABLE = '/8021qCf.cgi'
+
 SW_8021Q_CFG = '/8021qCf..'
 SW_8021Q_MEMBERSHIP = '/8021qMembe..'
 SW_8021Q_PVIDS = '/portPVID..'
diff -ruN prosafe-vlan-old/prosafe/switches/gs108ev3/switch.py prosafe-vlan-new/prosafe/switches/gs108ev3/switch.py
--- prosafe-vlan-old/prosafe/switches/gs108ev3/switch.py	2026-08-15 14:36:37.038824540 +0200
+++ prosafe-vlan-new/prosafe/switches/gs108ev3/switch.py	2026-08-13 23:58:53.971087623 +0200
@@ -8,7 +8,7 @@
 from bs4 import BeautifulSoup
 import requests
 
-from ..general import BaseSwitch, PortId, PvidConfig, SingleVlanConfig, VlanConfig, VlanId, VlanPortMembership
+from ..general import BaseSwitch, PortId, PvidConfig, SingleVlanConfig, VlanConfig, VlanId, VlanPortMembership, validate_password
 from .consts import *
 from .utils import password_kdf, simple_slug
 
@@ -56,12 +56,15 @@
         self._session_hash = None
 
     def login(self):
+        self.login_with_password(self._password)
+
+    def login_with_password(self, password: str):
         res = self._s.get(SW_LOGIN)
         soup = BeautifulSoup(res.text)
         random_number = soup.find(id=SW_FORM_RAND_ID).get('value', None)
         assert isinstance(random_number, str), "Cannot get salt/rand from form data, so cannot login!"
 
-        hashed_password = password_kdf(self._password, random_number)
+        hashed_password = password_kdf(password, random_number)
         login_form = {'password': hashed_password}
         res = self._s.post(SW_LOGIN, data=login_form)
         soup = BeautifulSoup(res.text)
@@ -80,6 +83,49 @@
         # logout don't have to succeed
         self._s.get(SW_LOGOUT)
 
+    def change_default_password(self, new_password: str):
+        assert validate_password(new_password), \
+            "New password does not meet the switch policy: 6-20 chars, one uppercase, one lowercase, one digit, allowed symbols !@#$%^&*()"
+        res = self._s.get(SW_PWD_CK)
+        soup = BeautifulSoup(res.text)
+        hash_ele = soup.find('input', id='hashEle')
+        assert hash_ele is not None, "Cannot get default-password-change hash token"
+        res = self._s.post(SW_CHANGE_DEFAULT_PWD, data={
+            'hash': hash_ele.get('value', ''),
+            'oldPassword': 'password',
+            'newPassword': new_password,
+        })
+        if 'TRUE' not in res.text:
+            raise RuntimeError(f"Default password change failed: {res.text[:200]}")
+
+    def change_password(self, old_password: str, new_password: str):
+        assert validate_password(new_password), \
+            "New password does not meet the switch policy: 6-20 chars, one uppercase, one lowercase, one digit, allowed symbols !@#$%^&*()"
+        res = self._s.get(SW_USER_PWD_PAGE)
+        soup = BeautifulSoup(res.text)
+        hash_ele = soup.find('input', id='hash')
+        assert hash_ele is not None, "Cannot get password-change hash token"
+        res = self._s.post(SW_USER_PWD, data={
+            'hash': hash_ele.get('value', ''),
+            'oldPassword': old_password,
+            'newPassword': new_password,
+            'reNewPassword': new_password,
+        })
+        err_ele = BeautifulSoup(res.text).find(id=SW_FORM_ERRMSG_ID)
+        err_msg = err_ele.get('value', '') if err_ele is not None else ''
+        if 'Password changed' not in res.text and ('FALSE' in res.text or err_msg):
+            raise RuntimeError(f"Password change failed: {err_msg or res.text[:200]}")
+
+    def enable_8021q_mode(self):
+        res = self._s.get(SW_8021Q_CFG)
+        soup = BeautifulSoup(res.text)
+        hash_ele = soup.find('input', id='hash')
+        assert hash_ele is not None, "Cannot get 802.1Q mode-change hash token"
+        res = self._s.post(SW_8021Q_ENABLE, data={
+            'status': 'Enable',
+            'hash': hash_ele.get('value', ''),
+        })
+
     def backup(self) -> bytes:
         r = self._s.get(SW_BACKUP)
         r.raise_for_status()
diff -ruN prosafe-vlan-old/prosafe/vlan_config.py prosafe-vlan-new/prosafe/vlan_config.py
--- prosafe-vlan-old/prosafe/vlan_config.py	2026-08-15 14:36:37.039032750 +0200
+++ prosafe-vlan-new/prosafe/vlan_config.py	2026-08-15 14:36:11.593870557 +0200
@@ -2,7 +2,7 @@
 
 from collections import defaultdict
 import tomllib
-from typing import List, Dict
+from typing import List, Dict, Optional
 from typing_extensions import Annotated
 
 from pydantic import BaseModel, validate_call
@@ -43,7 +43,7 @@
 
 class SwitchVlanConfig(BaseModel):
     address: str
-    password: str
+    password: Optional[str] = None
     model: SwitchModel
     ports: Dict[PortId, _PortVlanConfig]
 

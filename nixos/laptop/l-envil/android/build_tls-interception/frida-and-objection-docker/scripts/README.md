# Pentesting mobile apps.


You can overwrite the connect with an proxy object, which will redirect every request to a proxy (mine is at 10.14.0.54).

Might be only this specific app that this works, but for me this worked aight.


- state=unverified | target=hostile-node-overlay-acceptance-proof | hypothesis=`clab-hostile-node01` and `nixos-hostile-node01` are endpoint acceptance contexts for hostile overlay egress and leak denial; they must not require client-fixture route hacks. | evidence=2026-05-28 after router-side live repairs only, `clab-hostile-node01` passed `ping -c2 1.1.1.1`, `ping -6 -c2 2606:4700:4700::1111`, and modeled DNS `dig +time=2 +tries=1 @10.90.10.1 google.com A`. Direct public DNS leak attempts to `@1.1.1.1` and `@2606:4700:4700::1111` timed out. `traceroute -n -w 1 -q 1 1.1.1.1` crossed the local hostile path, then `100.96.10.3`, `10.80.0.11`, and `10.80.0.4` before the Hetzner exit, proving overlay egress. No endpoint-local route, resolver, NAT66, or fixture patch was applied. | solution=Keep this endpoint command set as the FAT proof after durable model fixes: public IPv4 ping, public IPv6 ping with delegated GUA source, modeled DNS through `10.90.10.1`, direct public DNS denial, and overlay traceroute. Do not add endpoint-local default routes, DNS bypasses, NAT66 shortcuts, or client-fixture hacks to make it pass. | owning_repo=s-router-test-clients_validation_only | reason=The client fixture supplies endpoint evidence; the missing route/firewall/DNS contracts belong upstream of the client box.

- state=solved | target=s-sigma-hypervisor-dhcp-fixture | evidence=2026-05-28 current live `s-sigma` has DHCP/SLAAC management-network addressing (`10.20.10.100/24`, `fd42:dead:beef:10::/64`), default route `via 10.20.10.1 dev eth0 proto dhcp`, and link DNS servers `10.20.10.1` plus `fd42:dead:beef:10::1`. `dig +time=2 +tries=1 google.com A` through the local stub returns `NOERROR`. `nixos-admin-test` reaches `10.20.10.100` with `ping -c2`, while `nixos-client01` cannot ping `10.20.10.100`, proving the current admin-to-mgmt reachability shape without making `s-sigma` a router, access node, static endpoint, or public-exit exception. | reason=`s-sigma` is currently realized as the intended DHCP management host fixture. Local-record DNS for `s-sigma.lan` returned NXDOMAIN and is not claimed solved here unless a separate model requirement says that record must exist.

- state=solved | target=client-endpoint-debug-tools | evidence=2026-05-21 after the full rebuild-loop rebuilt `s-router-test-clients`, `nixos-container run <client> -- bash -lc 'command -v nmap'` returned `/run/current-system/sw/bin/nmap` for `clab-client01`, `clab-client02`, `nixos-client01`, `nixos-client02`, `clab-streaming01`, and `nixos-streaming-test`. | reason=`s-router-test-clients` owns endpoint fixture packages for live validation; missing endpoint tools are not network model semantics.

- state=solved | target=dmz-nebula-udp-fixture-socat-listener | evidence=2026-05-21 live `nixos-nebula01` had failed `nebula-fixture-udp.service` with `socat E parseopts_table(): option "fork" not supported with this address type`; committed patch changed the DMZ UDP fixture from `UDP6-RECV,...,fork` to `UDP6-LISTEN,reuseaddr,fork`, guarded by `network-codex-agent/tests/test-s-router-test-clients-socat-fixtures.sh`. | reason=`s-router-test-clients` owns endpoint DMZ service fixtures; this is not router, renderer, NFM, or CPM behavior.

- state=solved | target=site-scoped-client-fixture-bridges | evidence=2026-05-24 post-rebuild live `clab-client01` on `s-router-test-clients` had DHCP/SLAAC addressing on the shared client bridge (`10.20.20.103/24` and `fd42:dead:beef:20::/64`), default route via `10.20.20.1`, `ip route get 1.1.1.1` through `eth0`, and `ping -c3 -W2 1.1.1.1` succeeded 3/3. Direct `dig @1.1.1.1 example.com` timed out, preserving the public DNS leak block. | reason=The client box owns endpoint fixture realization. CLAB cross-site client fixtures consume the shared bridge dynamically instead of inventing a stale static `10.50.20.1` gateway.

- state=unverified | target=core-nebula-manual-overlay-underlay-gate | hypothesis=After a clean FAT rebuild, `nixos-router-core-nebula` is a DHCP/SLAAC LAN client for underlay internet, so the manual Nebula service may only be started after the underlay firewall matches that shape and does not leak payload or public DNS. | evidence=2026-05-28 clean `scripts/s-router-full-lab-rebuild-loop.sh` reached live validation and failed. Fresh live checks showed `nixos-router-core-nebula` has underlay internet on `client` (`ip route get 1.1.1.1` -> `via 10.20.20.1 dev client`) but `ip route get 10.90.10.1` also selected `via 10.20.20.1 dev client` because the loop skipped the manual service with `skipping core-nebula manual overlay start; s-router-test has no network-renderer-nebula artifact`. The host only exposes `/etc/network-renderer/network-renderer-nixos.json`, while the runtime target itself exposes `s-router-nebula-render-node.service`, `s88-provider-interface-overlay-west.service`, and generated provider route/rule units. Its nft input rules allowed overlay underlay UDP only on `iifname "upstream" udp dport { 443, 4242 }`, while the active underlay WAN path is `client`. After starting `s-router-nebula-render-node.service`, `overlay-west` appeared with `100.96.10.1/24`, Nebula handshakes completed with Hetzner, and `ip route get 10.90.10.1` plus `ip -6 route get fd42:dead:cafe:10::1` selected `dev overlay-west`. 2026-05-28 focused loop guard now starts the manual service only after core underlay ping to raw `1.1.1.1` passes and direct public DNS to `@1.1.1.1` does not return `NOERROR`: `bash tests/test-s-router-test-live-probe-fails-hard.sh` passed. | temporary_patch=Manual service start only; no route/nft live patch is accepted as durable evidence. | solution=The next clean full FAT loop must exercise the manual service through the harness by detecting the container-level service, not only a host-level renderer artifact, and prove the same overlay route without copied configs or manual route/nft edits. | owning_repo=network-codex-agent+network-renderer-nixos_runtime_check | reason=`network-codex-agent` owns the manual-service validation workflow and feature gate; `network-renderer-nixos` owns provider-created interface route/rule materialization after the service creates `overlay-west`.

- state=solved | target=runtime-debugger-host-timeout | hypothesis=Runtime artifact comparison must not hang silently while sweeping a host or container set. | evidence=2026-05-28 rerun completed without the previous silent hang: `python3 scripts/runtime-debugger/main.py --s-router-test s-router-test --s-router-clab s-router-clab --hetzner-host 95.217.189.33 --check routes --check runtime_signals` returned PASS for artifact paths, route checks, and runtime signals across `s-router-test`, `s-router-test-clients`, `s-router-clab`, and Hetzner. | solution=Keep the bounded runtime-debugger checks in the full FAT workflow; reopen only if a current run names a hanging host/check. | owning_repo=network-codex-agent | reason=The original timeout was not reproduced in the current preserved lab, so it is no longer a start blocker.

- state=unverified | target=hostile-overlay-egress-clean-chain | hypothesis=The hostile client egress leg must be fully emitted by the locked model/render/runtime chain, not by lab-local runtime commands. | evidence=2026-05-28 full `scripts/s-router-full-lab-rebuild-loop.sh` reached live validation and exited non-zero: hostile public-egress IPv6 failed, three-site allowed/denied/full-exposure/live-route validators failed, and `tests-from-s-router-test-clients` failed a policy IPv4 route-get check. Current live route proof required generated provider route/rule services to run after `overlay-west` exists; manual `systemctl start s88-provider-route-overlay-west-* s88-provider-policy-rule-overlay-west-*` moved core-nebula hostile public route selection from `client` to `overlay-west`, and tcpdump proved packets left local `overlay-west` and arrived at Hetzner `overlay-west`. The next split was remote route/firewall parity: `hetz-router-upstream ip route get 1.1.1.1 from 10.20.70.100 iif nebula-core` selected `via 10.80.0.4 dev core table 2001`, while nft allowed `nebula-core -> pol-dmz-ew`, matching the compiler path expectation `hetz-router-nebula-core -> hetz-router-upstream -> hetz-router-policy -> hetz-router-upstream -> hetz-router-core`. Renderer focused proof for the local provider-trigger atom passed after adding a provider-interface watcher: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-provider-overlay-static-policy-rules.sh`. | solution=Before FAT can be `OK`, a clean rebuild must emit provider-overlay route/rule activation, remote `nebula-core -> pol-dmz-ew` route/firewall parity, return routes, Hetzner core IPv4 SNAT only at public exit, no NAT66 for hostile GUA, resolver-service DNS exceptions before public-DNS leak drops, and matching nft forward legs. | owning_repo=network-renderer-nixos+network-control-plane-model_pending_artifact_split | reason=The local provider route/rule atom has a renderer fix; the remote Hetzner policy path still needs current artifact split before choosing CPM vs renderer.

- state=unverified | target=hostile-overlay-dns-policy-table-return-routes | hypothesis=Overlay edge policy tables must contain both remote overlay-prefix routes and local hostile-prefix return routes; otherwise DNS replies can loop on the wrong edge even when Nebula itself is online. | evidence=2026-05-28 after starting the generated manual Nebula service, `overlay-west` was online and base peer pings worked between `nixos-router-core-nebula` and `hetz-router-nebula-core`. Hostile DNS still timed out. Tcpdump proved `dig -b 10.20.70.1 @10.90.10.1 example.com A` reached `hetz-router-access-dmz` and Unbound replied, but replies looped on `hetz-router-nebula-core` `upstream` with TTL decrement. Temporary Hetz routes in tables `2000`/`2001` for `10.20.70.0/24 dev overlay-west` and `fd42:dead:beef:70::/64 dev overlay-west` moved replies to the overlay. Then local core-nebula still looped the response because `ip route get 10.20.70.1 from 10.90.10.1 iif overlay-west` selected `dev overlay-west table 2000`; temporary local routes in tables `2000`/`2001` for `10.20.70.0/24 via 10.10.0.17 dev upstream onlink` and `fd42:dead:beef:70::/64 via fd42:dead:beef:1000::11 dev upstream onlink` made hostile DNS A/AAAA and `run_sitec_streaming_probe` pass in the tainted lab. Artifact split shows Hetz overlay routes exist in NFM/CPM but are not usable in the selected policy tables, while local core-nebula upstream return routes for `10.20.70.0/24` and `fd42:dead:beef:70::/64` are absent from NFM/CPM. | temporary_patch=Only reversible `ip route replace` entries in runtime tables `2000`/`2001`; no NixOS-local route hack is accepted as durable. | solution=Patch the owning model layer so the locked chain emits the policy-table complement routes. The durable route-get checks are: Hetz `ip route get 10.20.70.1 from 10.90.10.1 iif upstream` -> `dev overlay-west`; local core-nebula `ip route get 10.20.70.1 from 10.90.10.1 iif overlay-west` -> `via 10.10.0.17 dev upstream`; same IPv6 pattern for `fd42:dead:beef:70::/64`. | owning_repo=network-forwarding-model+network-control-plane-model_pending_first_patch | reason=NixOS is only the runtime consumer here. The first missing local return route appears before rendering, and the Hetz route needs CPM/policy-table preservation rather than a NixOS-local route invention.

- state=unverified | target=core-nebula-loopback-dns | hypothesis=`nixos-router-core-nebula` must deny direct public DNS leaks while still resolving through its modeled local resolver path from loopback, and resolver egress must not use the stale p2p/service-origin source when core-nebula is a DHCP/SLAAC client-LAN underlay member. | evidence=2026-05-28 live core-nebula had DHCP/SLAAC underlay on `client`, `ping -c2 1.1.1.1` worked, and direct public DNS leaks still timed out (`dig +time=2 +tries=1 @1.1.1.1 google.com A` and `dig +time=2 +tries=1 @2606:4700:4700::1111 google.com AAAA`). Unbound was active and listening on `127.0.0.1:53`/`::1:53`, but `dig google.com A` timed out while `/etc/unbound/unbound.conf` pinned `outgoing-interface: 10.19.0.8` and `outgoing-interface: fd42:dead:beef:1900:0:0:0:8`. Direct modeled forwarding worked from the normal client underlay source (`dig +time=2 +tries=1 @10.20.10.1 google.com A` returned `NOERROR`). Removing only those two `outgoing-interface` pins from a copied runtime config and restarting Unbound made `dig google.com A` and `dig google.com AAAA` return `NOERROR` from `127.0.0.1#53`, while direct public DNS remained blocked. | temporary_patch=Backed up `/etc/unbound/unbound.conf` to `/run/unbound.conf.before-live-proof`, deleted the two loopback service-origin `outgoing-interface` lines, and restarted `unbound`. | solution=The locked model/render/runtime chain must emit resolver egress for the current realization: when core-nebula is a dynamic client-LAN underlay member, local resolver queries may listen on loopback but must forward through a routable modeled resolver lane/source, not the stale `10.19.0.8`/`fd42:dead:beef:1900::8` service-origin address unless matching return routes are also emitted and validated. Direct public DNS leak-deny rules must remain in place. | owning_repo=network-control-plane-model+network-renderer-nixos_pending_artifact_split | reason=The live patch proves the bad atom is resolver source selection/return routing, not Unbound availability and not a missing public DNS bypass.

- state=unverified | target=normal-endpoint-dns-and-ipv6-nat66-core-exit | hypothesis=Normal ULA-only endpoints that require IPv6 internet must use explicit core-exit NAT66 and the modeled resolver chain; direct endpoint public DNS must still be denied unless intent explicitly allows it. | evidence=2026-05-28 current live endpoint checks after the failed full loop: `nixos-client01` and `clab-client01` both have DHCP/SLAAC ULA client addresses on `10.20.20.0/24`, `ip route get 1.1.1.1` selects `via 10.20.20.1`, `ping -c2 1.1.1.1` passes, `dig google.com A` and `dig google.com AAAA` through `127.0.0.53` return `NOERROR`, and direct public DNS to `@1.1.1.1` and `@2606:4700:4700::1111` times out. The separate NAT66 atom still fails: `ip -6 route get 2606:4700:4700::1111` selects the RA default via the access router with ULA source, but `ping -6 -c2 2606:4700:4700::1111` loses 2/2 from both clients. Streaming is also still broken: `nixos-streaming-test` has `10.20.50.101/24` and ULA on streaming, but `ping -c2 1.1.1.1`, `ping -6 -c2 2606:4700:4700::1111`, and stub `dig google.com A/AAAA` all time out while direct public DNS is still blocked. | solution=The locked chain must emit normal-client public IPv4 egress, modeled resolver success, direct public DNS denial, explicit core-exit NAT66 for ULA-only IPv6 internet, and streaming public egress/DNS according to intent. After artifact split, the focused clean-loop checks are `nixos-client01`, `clab-client01`, and `nixos-streaming-test`: IPv4 ping, IPv6 ping, stub/model DNS, direct public DNS denial, route-get source/table, and nft NAT66 counters only at modeled core exits. | owning_repo=network-control-plane-model+network-renderer-nixos_pending_artifact_split | reason=Current live evidence split the atom: normal client IPv4/DNS/leak-deny works, normal client NAT66 does not, and streaming internet/DNS is still a separate route/firewall/DNS blocker.

- state=solved | target=manual-nebula-core-renderer-service | evidence=2026-05-28 current live `nixos-router-core-nebula` exposes the manual service, `systemctl is-active s-router-nebula-render-node.service` returns `active`, and `overlay-west` has `100.96.10.1/24` plus `fd42:dead:beef:ee::1/64`. Route-get to `10.90.10.1` selects `overlay-west`, and direct public DNS remains denied by nft leak rules. | reason=The manual service exists and can bring up the overlay after the underlay gate; hostile payload and DNS correctness remain separate model/render regressions above.

- state=solved | target=manual-nebula-profile-sops-materialization | evidence=2026-05-28 current Hetzner validator `95.217.189.33` has non-empty `/persist/nebula-runtime/profiles/hetz-router-lighthouse/{ca.crt,hetz-router-lighthouse.crt,hetz-router-lighthouse.key}` and `/persist/nebula-runtime/profiles/hetz-router-nebula-core/{ca.crt,hetz-router-nebula-core.crt,hetz-router-nebula-core.key}`; `nebula@runtime.service` is active in both `hetz-router-lighthouse` and `hetz-router-nebula-core`, and `hetz-router-nebula-core` has `overlay-west` with `100.96.10.3/24`. | reason=SOPS-backed Nebula profile materialization is no longer the active blocker in the current preserved Hetzner validator.

- state=solved | target=manual-nebula-runtime-underlay-pin | evidence=2026-05-28 core-nebula underlay route to current Hetzner `95.217.189.33` is pinned via `10.20.20.1 dev client`, while overlay payload routes are separate policy-table routes. The manual service stays active after overlay startup. | reason=The service can keep the public Nebula endpoint reachable over the underlay; remaining failures are overlay payload route/firewall/DNS contracts.

- state=solved | target=nebula-core-runtime-origin-underlay-egress | evidence=2026-05-24 post-rebuild live core-nebula had only the DHCP/SLAAC `client` underlay for WAN reachability, `/etc/systemd/network/10-client.network` set `DHCP=ipv4`, `IPv6AcceptRA=true`, and `LinkLocalAddressing=ipv6`, `ip route get 1.1.1.1` selected `via 10.20.20.1 dev client`, and direct public DNS leaked queries timed out under nft leak drops. 2026-05-28 recheck preserved that underlay shape while the overlay service was active. | reason=The stale direct downstream/p2p-underlay adapter is not present as the WAN path; core-nebula is a dynamic client LAN member for underlay.

- state=solved | target=service-dns-uplink-route | evidence=2026-05-17 `network-forwarding-model` commit `5736788` derives uplink lanes for service sources; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-service-source-uplink-lanes.sh` passed and emitted the `nixos-router-access-mgmt` access-uplink lanes for `isp-a` and `isp-b`. | reason=The durable bug was in NFM service-source lane derivation, not in `s-router-test` local glue or the NixOS renderer.

- state=solved | target=s-router-test-return-after-aborted-full-loop | evidence=2026-05-17 `network-codex-agent` commit `597a428` changed the CLAB child watcher to record CLAB failure, defer the fatal result, and only fail after the main `s-router-test` validation window. | reason=The orchestration bug was in full-loop child failure handling.

- state=solved | target=policy-dns-uplink-default-routes | evidence=2026-05-21 locked chain carried `network-forwarding-model` `c4b434b`, `network-control-plane-model` `a1ebd6d`, and `network-renderer-nixos` `16b6d00`; NFM, CPM, and renderer focused DNS/uplink tests passed. | reason=Typed service DNS egress defaults are derived upstream and preserved by CPM/renderer output.

- state=unverified | target=clab-live-validation-cross-host-trunk-contamination | hypothesis=After a clean rebuild, `s-router-clab` live-validation must deploy only nodes assigned to host `s-router-clab`, and stale foreign-host CLAB containers must be removed before `s-router-test` boots far enough to run IPv6 DAD on the shared tenant VLANs. | evidence=2026-05-29 full loop with `CLABGEN_DEPLOYMENT_HOST=s-router-clab` still failed first at site-C IPv6 DNS: `dig -b fd42:dead:beef:70::1 ... @fd42:dead:cafe:10::1` failed with `address not available`. Live `nixos-router-access-hostile` had `fd42:dead:beef:70::1/64 dadfailed tentative`; access admin/mgmt/dmz/streaming had the same DAD failure on `fd42:dead:beef:{15,10,30,50}::1`. `s-router-clab` was clean after its child loop completed (`bridge fdb show` only showed `aa:c1:ab:*` on VLAN 5, not tenant VLANs), but the DAD failures happened around `s-router-test` boot second 44, before the child CLAB post-check completed. Live proof after CLAB completion: deleting and re-adding the five tenant gateway addresses cleared DAD, and the exact failed site-C AAAA query from `fd42:dead:beef:70::1` returned `NOERROR`. | solution=Before rebooting `s-router-test`, the full loop must check the current `s-router-clab-container` Docker runtime and remove stale `clab-fabric-esp-nixos-*` and `clab-fabric-esp-hetz-*` containers from the shared trunk. The normal CLAB child loop may then redeploy the filtered `esp-clab-*` fabric. | owning_repo=network-codex-agent | reason=`network-renderer-containerlab-linux-backend` and NixOS host-deploy now filter the rendered fabric correctly; the remaining failure is full-loop orchestration ordering that lets stale pre-filter CLAB runtime poison IPv6 DAD before the current CLAB rebuild catches up.
- state=solved | target=vlan2-management-uplink-and-container-egress | evidence=2026-05-19 live `s-router-clab` SSH returned; host `vlan2` is routable, `eth0.2` is enslaved to `vlan2`, default route uses host `vlan2`, and the container has no `mgmt0`/`vlan2`. Live container state showed `eth0.4` enslaved to `br-uplink0`, so DHCP on `eth0.4` lost its lease and left the container without a default route. Durable config now keeps `eth0.4` as the VLAN bridge port and runs DHCPv4 on `br-uplink0`; `nix build path:/home/deadbeef/github/nixos#nixosConfigurations.s-router-clab.config.system.build.nixos-shell` passes. | reason=Host management must stay on host-only VLAN 2, while the CLAB container uses upstream VLAN 4 over `clab-trunk`; L3 egress belongs on the rendered uplink bridge, not the enslaved VLAN port.
- state=solved | target=host-adapter-freeze | evidence=2026-05-24 `host-adapter-guard.nix` blocked an unsafe change where the example render fixture removed live s-router-clab tenant VLANs from the guarded `eth0`/`clab0` trunk calculation. The host adapter still derives its VLAN trunk from the actual s-router-clab machine facts and must evaluate to hash `7bcb7933852106136a2d4c12129c7f28fcb7d741d6fbf3e9c3e68f33a4c3245b`: host management stays on `vlan2`, the nested CLAB management veth may only attach as `mgmt0` DHCP, and `clab0` carries VLANs 4/5 plus the live tenant VLANs 300/301/302/304/305/306/311, never VLAN2. | reason=The representative example is valid for boot-time renderer health, but the host adapter is local machine realization state. Removing real tenant VLANs from the trunk would make the assertion pass only by weakening the lab host rather than preserving the runtime contract.
- state=solved | target=clab-renderer-output-overlay-strip | evidence=2026-05-22 live `s-router-clab` had both `/run/s-router-clab/live-current/fabric.clab.yml` and a locally rewritten `fabric.no-overlay.clab.yml`; the local rewrite removed the rendered `clab.link.type: overlay` endpoint for `esp-clab-clab-router-core-nebula:eth2`, then `containerlab deploy` used the stripped file. `host-deploy.nix` and `container/deploy.nix` now deploy `fabric.clab.yml` directly, and `network-codex-agent` `tests/test-s-router-test-containerlab-gate.sh` fails if `fabric.no-overlay.clab.yml` is reintroduced. Live redeploy with `containerlab deploy -t /run/s-router-clab/live-current/fabric.clab.yml --reconfigure` created `core-nebula:eth2` on the overlay bridge. | reason=s-router-clab must consume renderer output as the test artifact. Stripping modeled overlay links in local NixOS glue hides renderer/model bugs and made the CLAB hostile path impossible before provider overlay runtime materialization could even be evaluated.

- state=solved | target=clab-render-live-current-topology-deploy | evidence=2026-05-28 current live `s-router-clab` host has `container@s-router-clab-container.service` active and `/run/s-router-clab/live-current -> /persist/s-router-clab/live-validation`. Inside `s-router-clab-container`, `containerlab inspect -t /persist/s-router-clab/live-validation/fabric.clab.yml --format json` reports the running fabric with `labPath` and `absLabPath` both equal to `/persist/s-router-clab/live-validation/fabric.clab.yml`, and the running container names include the current `clab-fabric-esp0xdeadbeef-site-a-*`, `clab-fabric-espbranch-site-b-*`, and site-c names. The live-validation topology was rendered on 2026-05-28 and is not the stale `/persist/s-router-clab/live-boot` deployment. | reason=The running Docker/containerlab fabric is now tied to the current live-validation topology path. Remaining endpoint egress failures belong to the route/firewall/DNS model/runtime checks, not this CLAB freshness gate.

- state=solved | target=clab-live-validation-source-mismatch | evidence=2026-05-29 current `/home/deadbeef/github/nixos/nixos/virtual-machine/nixos-shell-vm/s-router-clab/host-deploy.nix` renders from `labs/lab-s-sigma/s-router-test-three-site`, imports `getResolvedInventory.nix` with `{ renderer = "clab"; }`, and `network-codex-agent` guard `bash tests/test-s-router-test-containerlab-gate.sh` passed. `nix build --no-link --print-out-paths path:/home/deadbeef/github/nixos#nixosConfigurations.s-router-clab.config.system.build.nixos-shell` also passed. | reason=The host-side live-validation source now matches the FAT lab source; remaining CLAB failures must be proven against the current rendered lab rather than the stale example fixture.

- state=solved | target=hetzner-host-routed-gua-ipv6-forwarding | evidence=2026-05-28 live hostile GUA packet from `nixos-hostile-node01` reached `hetz-router-nebula-core` on `overlay-west` and selected the modeled ULA transit toward `hetz-router-core`, while the Hetzner NixOS host itself had `net.ipv4.ip_forward = 1` but `net.ipv6.conf.all.forwarding = 0` and `net.ipv6.conf.default.forwarding = 0`. The host already installed runtime routes for delegated `/64` prefixes via `br-wan`, so host IPv6 forwarding is required for routed hostile GUA return/egress and must not be replaced with NAT66. `host-networking.nix` now enables host IPv6 forwarding explicitly next to IPv4 forwarding. Focused proofs passed: `bash tests/test-s-router-hetzner-runtime-addresses-link-up.sh` from `network-codex-agent`, and `nix eval --json '(builtins.getFlake "path:/home/deadbeef/github/nixos").nixosConfigurations.s-router-hetzner-anywhere.config.boot.kernel.sysctl."net.ipv6.conf.all.forwarding"'` returned `1`. | expected=The Hetzner validator host must forward routed delegated GUA prefixes between the public interface and `br-wan` without assigning those GUAs to infrastructure hops beyond required host /128 route anchors and without NAT66. | owner=nixos | reason=This is host machine kernel/runtime forwarding for the disposable Hetzner validation host. The network model and renderers own route/firewall contracts inside the modeled containers; the NixOS host owns whether Linux is allowed to forward the routed provider prefixes it installs.

- state=solved | target=hetzner-host-composition-runtime-network-policy-spaghetti | evidence=2026-05-21 Hetzner host composition was split into focused modules: `host-composition.nix` 146 lines, `runtime-addresses.nix` 100 lines, and `host-networking.nix` 63 lines. `network-codex-agent` `tests/test-s-router-hetzner-runtime-addresses-link-up.sh` now checks the split runtime-address module raises `br-wan`, waits with `networkctl status br-wan --no-pager`, fails hard with `br-wan did not reach configured state before applying Hetzner runtime routes`, and still installs routed IPv6 prefixes via `br-wan`; `nix eval .#nixosConfigurations.s-router-test.config.system.build.toplevel.drvPath` passes with the staged local NixOS harness. | reason=Host-only machine networking is separated from composition, and the boot-order route materialization guard is explicit instead of being hidden inside a mega-module.
- state=solved | target=hetzner-public-runtime-forward-port-hardcode | evidence=2026-05-21 full rebuild-loop failed Site-C DNS-over-Nebula while `hetz-router-nebula-core` listened on `172.31.254.4:443` but host nft `s88-public-runtime-forward-0` still matched UDP/4243. Live replacing only the public runtime DNAT/forward dport with 443 made `nixos-router-core-nebula` and `hetz-router-nebula-core` peer pings succeed and made the original hostile access DNS probe return A/AAAA answers. `host-composition.nix` no longer passes `runtimeForwardDports = [ 4243 ]`; the port comes from `network-renderer-nebula` `runtimeNode.service.port`. | reason=The Hetzner host may wire the rendered public ingress module, but it must not restate provider runtime ports that already exist in the Nebula model output.

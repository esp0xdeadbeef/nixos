{ config, pkgs, ... }: {

  environment.systemPackages = with pkgs; [
    font-awesome  # Corrected from `fonts.font-awesome`
  ];

  # Ensure the font is available system-wide
  fonts.packages = with pkgs; [
    font-awesome  # Corrected from `fonts.font-awesome`
  ];

}

